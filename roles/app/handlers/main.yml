---
- name: "Restart caddy container"
  community.docker.docker_compose_v2:
    project_src: "{{ app_dir }}"
    files:
      - docker-compose.yml
    state: present
    recreate: always
  when: not ansible_check_mode