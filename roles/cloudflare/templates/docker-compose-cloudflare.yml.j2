version: '3.8'

services:
  cloudflare-tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate --edge-ip-version 4 run --token {{ vault_cloudflare_tunnel_token }}
    restart: unless-stopped
    networks:
      - wikijs_default

networks:
  wikijs_default:
    external: true